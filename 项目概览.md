# 中文论文英译工作流模板

一个**可复用**的中→英学术论文翻译工作流。把待翻译的中文 docx 放入 [input/](./input/)，解构提取原文（**含表格**）→ 翻译成英文 → 用 `humanizer` skill 润色去除 AI 痕迹 → 用 `paper-detection` skill 检测 AI 率，全程产出 Markdown 文档，并保留可选的 docx 排版能力。

> 与旧方案的区别：
> 1. 不再把英文译文硬编码到 Python 脚本里，而是统一用 Markdown 文件承载翻译内容。翻译新论文时只需替换 `translation.md`，无需改任何脚本。
> 2. **新增润色环节**：翻译生成的原始英文译文不再直接生成 docx，而是先用 `humanizer` skill 润色，产出更像人类手笔的英文译文 md。
> 3. **新增对照与检测环节**：自动生成「润色前后逐段对照」md，并对润色后译文运行 AI 痕迹检测，出具检测报告 md。
> 4. **完整支持表格**：Word 表格自动转为 Markdown 表格格式，翻译后再转回 Word 原生表格，表头自动加粗，保留表格样式。

---

## 一、目录结构

```
Document_translation/
├── 项目概览.md                      ← 本文档
├── requirements.txt                ← Python 依赖清单
├── extract_text.py                 ← 工具脚本：docx → 纯文本（保留上下标 + 表格结构）
├── build_docx.py                   ← 工具脚本：markdown → docx（支持上下标 + 表格）
├── <输入文件名>_translation.md          ← 【原始译文】解构 + 翻译后的英文原文
├── <输入文件名>_translation_humanized.md ← 【润色译文】humanizer 润色后的英文译文
├── <输入文件名>_humanize_comparison.md   ← 【对照表】润色前后逐段对照（AI 痕迹 + 主要改动）
├── <输入文件名>_ai_detection_report.md  ← 【检测报告】paper-detection 对润色后译文的检测结果
├── input/                          ← 放待翻译的中文 docx
├── temp/                           ← 中间产物（提取的纯文本等）
└── output/                         ← 最终生成的英文 docx（可选）
```

**文件命名说明：** 所有生成的文件都以输入文件名作为前缀，例如输入文件为 `input/我的论文.docx` 时：
- 提取的文本：`temp/我的论文_extracted_text.txt`
- 原始译文：`我的论文_translation.md`
- 润色译文：`我的论文_translation_humanized.md`
- 对照表：`我的论文_humanize_comparison.md`
- 检测报告：`我的论文_ai_detection_report.md`
- 最终 docx：`output/我的论文_translation_humanized.docx`（或根据输入 md 文件名自动推导）

---

## 二、环境准备（首次使用）

本地有 miniconda。请创建一个独立的 Python 虚拟环境后再安装依赖：

```bash
# 1. 创建虚拟环境（环境名待用户指定）
conda create -n <环境名> python=3.11 -y
conda activate <环境名>

# 2. 安装依赖
pip install -r requirements.txt
```

> `paper-detection` skill 另有独立的 conda 环境 `PDF_AIGC`（已含 spacy + en_core_web_sm），由该 skill 自行调用，与本项目的翻译环境互不影响。

---

## 三、翻译一篇新论文的完整流程

整体分为六个步骤：**解构提取 → 翻译 → 润色 → 对照 → 检测 →（可选）排版**。

### 步骤 1：放入中文原稿

把待翻译的中文 docx 复制到 [input/](./input/) 目录，例如：

```
input/我的新论文-初稿.docx
```

### 步骤 2：解构提取中文纯文本

```bash
python extract_text.py input/我的新论文-初稿.docx
```

- **输入：** `input/我的新论文-初稿.docx`
- **输出：** `temp/我的新论文-初稿_extracted_text.txt`（解构出来的中文纯文本，供翻译时阅读对照）
- **上下标保留：** 原始 docx 中的 Word 原生上下标会自动转为 Unicode 上下标字符（如 `H₂O`、`Cu²⁺`），在后续翻译中直接沿用即可。
- **表格保留：** 原始 docx 中的表格会被转换为 Markdown 表格格式（`| 列1 | 列2 |` 形式），在后续翻译中保持这个格式即可。

### 步骤 3：翻译生成原始英文译文

创建 `<输入文件名>_translation.md`（例如 `我的新论文-初稿_translation.md`），按其中模板结构把英文译文逐节填入：

- `# 标题` → 一级标题
- `## 子标题` → 二级标题
- `### 子子标题` → 三级标题
- 普通段落直接写英文文字，段落间留空行
- `> 引用块` 会被忽略（仅作注释用途）
- 表格使用 Markdown 表格格式（`| 列1 | 列2 |` 形式）
- `# References` 章节内容**保持原中文格式不翻译**，直接粘贴原参考文献即可

> **上下标写法：** 化学式和离子符号中的上下标请直接使用 Unicode 字符（如 `H₂O`、`Cu²⁺`、`cm⁻¹`），这会在最终 docx 中自动转为 Word 原生上下标格式。常见的映射：`0123456789` → 上标 `⁰¹²³⁴⁵⁶⁷⁸⁹`、下标 `₀₁₂₃₄₅₆₇₈₉`；`+-` → 上标 `⁺⁻`、下标 `₊₋`。提取出的中文文本中已自带 Unicode 上下标，翻译时直接沿用即可。

> **表格写法：** 使用标准 Markdown 表格语法，第一行为表头，第二行为分隔符（`| --- | --- |`），后续为数据行。例如：
> ```markdown
> | 年份 | 方法名称 | 检测范围 |
> | --- | --- | --- |
> | 2018 | AMO-MIP | 0.1 – 2.6 nM |
> | 2020 | GO/CdTe | 5–1000 nM |
> ```
> 提取出的中文文本中已自带 Markdown 表格格式，翻译时只需翻译表格内容，保持表格结构不变即可。

> 本步骤产出的 `<输入文件名>_translation.md` 是**原始英文译文（解构后翻译得到的原文）**，它是润色的输入。**此阶段不生成 docx。**

### 步骤 4：用 humanizer 润色英文译文（新增）

对 `<输入文件名>_translation.md` 中的英文译文调用 `/humanizer` skill 进行润色，去除 AI 写作痕迹（套话短语、过度强调、-ing 堆砌、em dash、规则三连、同义词循环等）：

- **输入：** `<输入文件名>_translation.md`（原始英文译文）
- **输出：** `<输入文件名>_translation_humanized.md`（润色后的英文译文 md）
- **处理粒度：** 逐段润色，保证段落数与原文一致；`# References` 章节原样保留不润色。

> 润色目标是「更像人类手笔」，而非改变学术含义。专业术语、数值、单位、图表编号（Figure 1、Table 1 等）必须原样保留。

> ⚠️ **务必分章节分批润色，不要一次性对整篇论文调用 humanizer。**
> 原因：humanizer skill 注入的规则很长（30+ 类 AI 痕迹），叠加整篇论文的长输入 + 「润色 + 对照」的复杂产物，会让单次推理时间过长甚至卡死。把论文按章节切成小批，每批输入小、产出快、可控。
>
> **分批原则（按论文实际结构自行划分，不必拘泥固定批数）：**
> - 以论文自身的一级 / 二级标题为切分边界，**每批 1 个章节或几个相邻小节**，控制单批篇幅大致在 3–6 段或 800–1500 词。
> - 篇幅长、信息密集的章节（如 Results 下含多个小节、含大表格的小节）**单独成批**；篇幅短的相邻小节可合并成一批。
> - `# References` **始终单独作为最后一批，原样追加、不润色**。
> - 典型学术论文可大致切成：摘要类（Title/Abstract/Keywords）→ 引言 → 方法/实验 → 结果与讨论（按小节再拆若干批）→ 结论 → References。具体批数随论文长度和章节多少浮动。
>
> **每批操作：**
> 1. 第一批用 Write 创建 `<输入文件名>_translation_humanized.md`，后续批次用 Edit 在文件末尾追加（保持章节顺序）。
> 2. 同步把该批的对照内容追加进 `<输入文件名>_humanize_comparison.md`（见步骤 5）。
> 3. 用 TodoWrite 跟踪批次进度，逐批标记完成。
>
> **全部批次完成后做一次正文校验：** 用 grep 检索译文中的 `—`（em dash），确认正文段落已无修辞性破折号；数值/波长范围里的 en dash `–`（如 `0.01–15 μM`、`300–400 nm`）属数据记号，应保留，不要替换。固定小标题与 References 中的 dash 也保留。
>
> 另需校验化学式下标：在 md 中搜索 `([A-Z][a-z]?)(\d+)` 类模式（如 `H2O`、`SO4`、`C6H6O3`），确认元素符号后的数字已使用 Unicode 下标而非普通 ASCII 数字。若发现遗漏，手动将数字替换为对应 Unicode 下标字符（`₂₃₄₆` 等）。

### 步骤 5：生成润色前后对照表（新增）

在润色的同时，生成 `<输入文件名>_humanize_comparison.md`，逐段列出**润色前后英文对照**：

- **输入：** `<输入文件名>_translation.md` + `<输入文件名>_translation_humanized.md`
- **输出：** `<输入文件名>_humanize_comparison.md`
- **每段需包含：**
  1. 段落定位（章节 / 段序号）
  2. 润色前英文（原始译文）
  3. 润色后英文（humanizer 结果）
  4. **识别到的 AI 痕迹**（命中的具体 humanizer 特征，如「套话短语 play a crucial role」「em dash」「规则三连」）
  5. **主要改动说明**（这一段做了哪些关键改写）

对照表的推荐格式见下方[第五节](#五润色对照表-humanize_comparisonmd-格式)。

> 配合步骤 4 的分批策略：**对照表随每批润色同步追加**——每完成一批章节的润色，就把该批各段的「润色前 / 润色后 / AI 痕迹 / 主要改动」追加进 `<输入文件名>_humanize_comparison.md`，而不是等整篇润色完再统一写，避免重复阅读长文本。文末统一注明「References：原样保留，未润色」，并可加一张总体改动小结表。

### 步骤 6：对润色后译文做 AI 痕迹检测（新增）

对 `<输入文件名>_translation_humanized.md` 调用 `/paper-detection` skill 进行**全文逐句检测**，出具检测报告：

- **输入：** `<输入文件名>_translation_humanized.md`（润色后的英文译文）
- **输出：** `<输入文件名>_ai_detection_report.md`
- **报告需包含：**
  1. 全文综合 AI 概率 + 一句话定性（偏 AI / 偏人类 / 混合）
  2. 逐句概率与触发理由（🔴≥70% / 🟡40-70% / 🟢<40%）
  3. 文档级信号（句长突发度、句首过渡词占比、具体性密度等）
  4. 免责说明（本工具为启发式信号，不能作为学术不端证据）

> 该 skill 优先直接调用其封装好的 `detect.py` 脚本得到可复现结果，不凭语感打分。

### 步骤 7（可选）：生成英文 Word 文档

若仍需最终排版的 docx，可对润色后的译文运行：

```bash
python build_docx.py <输入文件名>_translation_humanized.md
```

例如：
```bash
python build_docx.py 我的新论文-初稿_translation_humanized.md
```

- **输入：** `<输入文件名>_translation_humanized.md`
- **输出：** `output/<输入文件名>_translation_humanized.docx`（自动根据输入 md 文件名生成）
- 字体 Times New Roman、正文 12 pt、参考文献 10 pt。
- **上下标自动转换**：译文中的 Unicode 上下标字符（如 `²⁺⁻¹₂₃` 等）会自动转为 Word 原生上下标格式，无需手动调整。例如 `Cu²⁺` → Cu<sup>2+</sup>、`cm⁻¹` → cm<sup>-1</sup>、`H₂O` → H<sub>2</sub>O。
- **表格自动转换**：译文中的 Markdown 表格会自动转为 Word 原生表格格式，表头自动加粗，应用 Word 表格样式。

打开生成的 docx 后核对：标题 / Abstract / Keywords 是否齐全、章节编号是否正确、字体字号是否合规、图表编号是否保留、上下标是否正确显示为 Word 原生格式、表格是否正确转换并格式良好。

---

## 四、文件流转图

```
input/我的新论文.docx
       │
       │  python extract_text.py          （步骤 2：解构提取）
       ▼
temp/我的新论文_extracted_text.txt            ── 翻译时对照阅读
       │
       │  人工 / AI 翻译                    （步骤 3：翻译）
       ▼
我的新论文_translation.md                     ── 原始英文译文
       │
       │  /humanizer  skill                （步骤 4 + 5：润色 + 对照）
       ├──────────────► 我的新论文_translation_humanized.md   ── 润色后英文译文
       └──────────────► 我的新论文_humanize_comparison.md      ── 润色前后逐段对照
                                │
                                │  /paper-detection skill   （步骤 6：检测）
                                ▼
                        我的新论文_ai_detection_report.md        ── AI 痕迹检测报告
                                │
                                │  python build_docx.py（可选，步骤 7）
                                ▼
                        output/我的新论文_translation_humanized.docx
```

---

## 五、润色对照表 `humanize_comparison.md` 格式

建议每段用如下结构，便于核对每处改动：

```markdown
## 段落 N（章节：Introduction）

**润色前（原始译文）：**
> This study delves into a wide range of factors that play a crucial role in ...

**润色后（humanizer）：**
> This study examines several factors that affect ...

**识别到的 AI 痕迹：**
- 套话短语：`delve into`、`play a crucial role`、`a wide range of`
- 规则三连：结尾三个并列名词
- em dash：1 处

**主要改动：**
- 用 `examines` 替换 `delves into`，去除套话
- 拆分长句，恢复句长长短交错
- 删除 em dash，改用句号
```

> References 章节不参与润色与对照，可在对照表末尾注明「References：原样保留，未润色」。

---

## 六、翻译原则约定

1. **参考文献保持原样**：References 部分不翻译、不润色，保留原中文版格式（含 DOI、作者、期刊等）。
2. **专业术语规范**：常见英文缩写（如 BSA-AuNCs、HPLC、LC-MS/MS、XPS、CIE 等）直接使用。
3. **学术风格**：Nature 系列期刊风格，Times New Roman，正文 12 pt，参考文献 10 pt。
4. **图表编号保留**：Figure 1–N、Table 1–N、Scheme 1、Figure S1– 等在正文中保留以便对照。
5. **润色不改义**：humanizer 只去 AI 痕迹、调整表达，不得改变实验数据、结论与术语。
6. **表格格式统一**：所有表格使用 Markdown 标准语法，翻译时保持表格结构，只翻译单元格内容。

---

## 七、表格处理说明（重要）

### 表格在工作流中的处理

本工作流已完整支持 Word 表格的提取、翻译和生成：

1. **提取阶段**（`extract_text.py`）
   - Word 表格 → Markdown 表格
   - 自动识别表格行列结构
   - 保留单元格内的上下标字符
   - 保持文档中段落和表格的原始顺序

2. **翻译阶段**（`translation.md`）
   - 保持 Markdown 表格结构不变
   - 只翻译表格单元格内的文字内容
   - 表头和数据行都需要翻译
   - 分隔符行（`| --- | --- |`）保持不变

3. **润色阶段**（`/humanizer`）
   - 表格内容与正文一样参与润色
   - 数值、单位、化学式等保持不变
   - 表格结构完全保留

4. **生成阶段**（`build_docx.py`）
   - Markdown 表格 → Word 原生表格
   - 表头自动加粗
   - 应用 Word 表格样式（Light Grid Accent 1）
   - 单元格内上下标自动转换

### Markdown 表格语法示例

```markdown
| 年份 | 方法名称 | 检测范围 | LOD |
| --- | --- | --- | --- |
| 2018 | AMO-MIP | 0.1 – 2.6 nM | 73 pM |
| 2020 | GO/CdTe | 5–1000 × 10⁻¹¹ mol/L | 9.2 × 10⁻¹² mol/L |
```

**注意事项：**
- 第一行是表头
- 第二行是分隔符（每列用 `---` 表示）
- 每行的列数必须一致
- 单元格内可以包含 Unicode 上下标字符
- 不支持单元格合并（Word 的合并单元格会被拆分）

### 表格翻译示例

**中文原表（提取后）：**
```markdown
| 年份 | 方法名称 | 检测限 |
| --- | --- | --- |
| 2022 | 碳点荧光探针 | 9.2 nM |
```

**英文译文（翻译后）：**
```markdown
| Year | Method Name | LOD |
| --- | --- | --- |
| 2022 | Carbon dots fluorescent probe | 9.2 nM |
```

---

## 八、常见操作

| 需求 | 操作 |
|------|------|
| 翻译新论文 | 替换 `input/` 中的 docx，创建 `<输入文件名>_translation.md`，依次走步骤 2–6 |
| 修改某一章节 | 编辑 `<输入文件名>_translation.md` 对应段落，对该段重跑 humanizer，并同步更新对照表与检测报告 |
| 只重新润色 | 对 `<输入文件名>_translation.md` 重跑 `/humanizer`（**按步骤 4 的批次分章节调用，勿整篇一次性跑**），覆盖 `<输入文件名>_translation_humanized.md` 与 `<输入文件名>_humanize_comparison.md` |
| 只重新检测 | 对 `<输入文件名>_translation_humanized.md` 重跑 `/paper-detection`，覆盖 `<输入文件名>_ai_detection_report.md` |
| 生成最终 docx | `python build_docx.py <输入文件名>_translation_humanized.md` |
| 调整字体/字号 | 编辑 [build_docx.py](./build_docx.py) 顶部的 `DEFAULT_FONT` / `BODY_SIZE` / `REF_SIZE` |
| 清理临时文件 | 删除 `temp/` 目录中的内容 |

---

## 九、脚本与 Skill 说明

| 组件 | 作用 | 是否需要修改 |
|------|------|--------------|
| [extract_text.py](./extract_text.py) | 解压 docx 并解析其中的 `word/document.xml`，按段落和表格顺序输出；自动将 Word 中的上下标 run 转为 Unicode 上下标字符，表格转为 Markdown 表格格式，确保后续流程的上下标和表格信息不丢失 | 否 |
| [build_docx.py](./build_docx.py) | 解析 markdown，按 Heading / 段落 / 表格构建 Word 文档；自动将 Unicode 上下标字符（`⁰¹²³⁴⁵⁶⁷⁸⁹⁺⁻` / `₀₁₂₃₄₅₆₇₈₉`）转为 Word 原生 superscript / subscript 格式，Markdown 表格转为 Word 原生表格 | 仅在需要调整全局样式时 |
| `/humanizer` skill | 识别并去除英文文本的 AI 写作痕迹，使其更像人类手笔 | 否（外部 skill） |
| `/paper-detection` skill | 对英文学术文本逐句评估 AI 生成可能性，出具综合概率与逐句理由 | 否（外部 skill） |

两个 Python 脚本均为模板的固定组成部分，对任何论文都通用。**每篇新论文唯一需要手工产出的内容是 `translation.md` 的英文译文**，其余 `translation_humanized.md`、`humanize_comparison.md`、`ai_detection_report.md` 均由对应 skill 生成。

---

*模板生成日期：2026/06/22*
*流程增强（润色 / 对照 / 检测）：2026/06/23*
*流程增强（humanizer 分章节分批润色，避免长文本卡死）：2026/06/23*
*功能增强（build_docx 支持 Unicode 上下标自动转 Word 原生格式）：2026/06/23*
*功能增强（extract_text 和 build_docx 支持表格提取和生成）：2026/06/24*
*功能增强（基于输入文件名的自动文件命名）：2026/06/24*
