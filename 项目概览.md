# 中文论文英译工作流模板

一个**可复用**的中→英学术论文翻译工作流。把待翻译的中文 docx 放入 `input/`，解构提取原文（**含表格**）→ 翻译成英文 → 用 `humanizer` skill 润色去除 AI 痕迹 → 用 `paper-detection` skill 检测 AI 率，全程产出 Markdown 文档，并保留可选的 docx 排版能力。

---

## 一、目录结构

```
Document_translation/
├── 项目概览.md                      ← 本文档
├── requirements.txt                ← Python 依赖清单
├── extract_text.py                 ← 工具脚本：docx → 纯文本（保留上下标 + 表格结构）
├── build_docx.py                   ← 工具脚本：markdown → docx（支持上下标 + 表格）
├── input/                          ← 放待翻译的中文 docx
│   └── 我的论文.docx
└── output/                         ← 所有生成文件的根目录
    └── 我的论文/                    ← 以输入文件名命名的项目目录
        ├── 我的论文_extracted_text.txt              ← 提取的纯文本
        ├── 我的论文_translation.md                  ← 【原始译文】解构 + 翻译后的英文原文
        ├── 我的论文_translation_humanized.md        ← 【润色译文】humanizer 润色后的英文译文
        ├── 我的论文_humanize_comparison.md          ← 【对照表】润色前后逐段对照（AI 痕迹 + 主要改动）
        ├── 我的论文_ai_detection_report.md         ← 【检测报告】paper-detection 对润色后译文的检测结果
        └── 我的论文_translation_humanized.docx     ← 最终生成的英文 docx（可选）
```

---

## 二、翻译一篇新论文的完整流程

> **⚠️ 首次使用前置条件：**
> 在执行以下流程之前，**必须先完成环境配置**。AI 助手应首先检查 `.project_env.json` 是否存在：
>
> - 若不存在：Read `docs/环境准备.md`，按其中流程完成环境配置
> - 若存在：可直接进行翻译流程
> - 若通过 CLI 工具、脚本或 AI 助手自动化执行后续流程：**不要在程序中使用 `conda activate` 切换环境**；应直接使用目标环境的 Python 绝对路径调用脚本。具体约定见 `docs/环境准备.md`

整体分为七个步骤：**解构提取 → 翻译 → 润色 → 对照 → 检测 →（可选）排版**。

### 步骤 1：放入中文原稿

把待翻译的中文 docx 复制到 `input/` 目录，例如：

```
input/我的新论文-初稿.docx
```

### 步骤 2：解构提取中文纯文本

```bash
python extract_text.py input/我的新论文-初稿.docx
```

- **输入：** `input/我的新论文-初稿.docx`
- **输出：** `output/我的新论文-初稿/我的新论文-初稿_extracted_text.txt`
- **上下标保留：** Word 原生上下标自动转为 Unicode 上下标字符（如 `H₂O`、`Cu²⁺`），后续翻译中直接沿用。
- **表格保留：** Word 表格转换为 Markdown 表格格式（`| 列1 | 列2 |` 形式），后续翻译中保持格式不变。

### 步骤 3：翻译生成原始英文译文

在 `output/我的新论文-初稿/` 目录下创建 `我的新论文-初稿_translation.md`，按其中模板结构把英文译文逐节填入：

- `# 标题` → 一级标题
- `## 子标题` → 二级标题
- `### 子子标题` → 三级标题
- 普通段落直接写英文文字，段落间留空行
- `> 引用块` 会被忽略（仅作注释用途）
- `# References` 章节内容**保持原中文格式不翻译**，直接粘贴原参考文献即可

> **上下标写法：** 化学式和离子符号中的上下标请直接使用 Unicode 字符（如 `H₂O`、`Cu²⁺`、`cm⁻¹`），这会在最终 docx 中自动转为 Word 原生上下标格式。提取出的中文文本中已自带 Unicode 上下标，翻译时直接沿用即可。

> **📖 按需参考：** 若原文含表格，Read `docs/表格处理说明.md` 了解表格翻译规范。

> 本步骤产出的 `output/<输入文件名>/<输入文件名>_translation.md` 是**原始英文译文（解构后翻译得到的原文）**，它是润色的输入。**此阶段不生成 docx。**

### 步骤 4：用 humanizer 润色英文译文

对 `output/<输入文件名>/<输入文件名>_translation.md` 中的英文译文调用 `/humanizer` skill 进行润色，去除 AI 写作痕迹（套话短语、过度强调、-ing 堆砌、em dash、规则三连、同义词循环等）：

- **输入：** `output/<输入文件名>/<输入文件名>_translation.md`（原始英文译文）
- **输出：** `output/<输入文件名>/<输入文件名>_translation_humanized.md`（润色后的英文译文 md）
- **处理粒度：** 逐段润色，保证段落数与原文一致；`# References` 章节原样保留不润色。

> 润色目标是「更像人类手笔」，而非改变学术含义。专业术语、数值、单位、图表编号（Figure 1、Table 1 等）必须原样保留。

> ⚠️ **务必分章节分批润色，不要一次性对整篇论文调用 humanizer。**
> 原因：humanizer skill 注入的规则很长（30+ 类 AI 痕迹），叠加整篇论文的长输入 + 「润色 + 对照」的复杂产物，会让单次推理时间过长甚至卡死。把论文按章节切成小批，每批输入小、产出快、可控。
>
> **分批原则（按论文实际结构自行划分，不必拘泥固定批数）：**
>
> - 以论文自身的一级 / 二级标题为切分边界，**每批 1 个章节或几个相邻小节**，控制单批篇幅大致在 3–6 段或 800–1500 词。
> - 篇幅长、信息密集的章节（如 Results 下含多个小节、含大表格的小节）**单独成批**；篇幅短的相邻小节可合并成一批。
> - `# References` **始终单独作为最后一批，原样追加、不润色**。
> - 典型学术论文可大致切成：摘要类（Title/Abstract/Keywords）→ 引言 → 方法/实验 → 结果与讨论（按小节再拆若干批）→ 结论 → References。具体批数随论文长度和章节多少浮动。
>
> **每批操作：**
>
> 1. 第一批用 Write 创建 `output/<输入文件名>/<输入文件名>_translation_humanized.md`，后续批次用 Edit 在文件末尾追加（保持章节顺序）。
> 2. 同步把该批的对照内容追加进 `output/<输入文件名>/<输入文件名>_humanize_comparison.md`（见步骤 5）。
> 3. 用 TodoWrite 跟踪批次进度，逐批标记完成。
>
> **🤖 AI 助手自动化执行：**
>
> - **所有批次应连续自动完成，无需在每批之间停顿等待用户确认**。AI 助手应自动识别章节划分，逐批调用 humanizer，将润色结果追加到文件，并同步更新对照表和任务进���，直到所有批次完成。
> - 只有在遇到错误、不确定如何划分批次、或需要用户决策时才停下来询问。
> - 完成所有批次润色后，自动进入下一步骤（生成对照表总结 → AI 检测 → 生成 Word 文档），形成完整的自动化流程。
>
> **全部批次完成后做一次正文校验：** 用 grep 检索译文中的 `—`（em dash），确认正文段落已无修辞性破折号；数值/波长范围里的 en dash `–`（如 `0.01–15 μM`、`300–400 nm`）属数据记号，应保留，不要替换。固定小标题与 References 中的 dash 也保留。
>
> 另需校验化学式下标：在 md 中搜索 `([A-Z][a-z]?)(\d+)` 类模式（如 `H2O`、`SO4`、`C6H6O3`），确认元素符号后的数字已使用 Unicode 下标而非普通 ASCII 数字。若发现遗漏，手动将数字替换为对应 Unicode 下标字符（`₂₃₄₆` 等）。

### 步骤 5：生成润色前后对照表

在润色的同时，生成 `output/<输入文件名>/<输入文件名>_humanize_comparison.md`，逐段列出**润色前后英文对照**：

- **输入：** `output/<输入文件名>/<输入文件名>_translation.md` + `output/<输入文件名>/<输入文件名>_translation_humanized.md`
- **输出：** `output/<输入文件名>/<输入文件名>_humanize_comparison.md`
- **每段需包含：** 段落定位、润色前英文、润色后英文、识别到的 AI 痕迹、主要改动说明

> **📖 按需参考：** 执行本步骤前，Read `docs/对照表格式.md` 了解对照表的推荐格式。

> 配合步骤 4 的分批策略：**对照表随每批润色同步追加**——每完成一批章节的润色，就把该批各段的「润色前 / 润色后 / AI 痕迹 / 主要改动」追加进对照表，而不是等整篇润色完再统一写。文末统一注明「References：原样保留，未润色」，并可加一张总体改动小结表。

### 步骤 6：对润色后译文做 AI 痕迹检测

对 `output/<输入文件名>/<输入文件名>_translation_humanized.md` 调用 `/paper-detection` skill 进行**全文逐句检测**，出具检测报告：

- **输入：** `output/<输入文件名>/<输入文件名>_translation_humanized.md`（润色后的英文译文）
- **输出：** `output/<输入文件名>/<输入文件名>_ai_detection_report.md`
- **报告需包含：**
  1. 全文综合 AI 概率 + 一句话定性（偏 AI / 偏人类 / 混合）
  2. 逐句概率与触发理由（🔴≥70% / 🟡40-70% / 🟢<40%）
  3. 文档级信号（句长突发度、句首过渡词占比、具体性密度等）
  4. 免责说明（本工具为启发式信号，不能作为学术不端证据）

> 该 skill 优先直接调用其封装好的 `detect.py` 脚本得到可复现结果，不凭语感打分。

### 步骤 7（可选）：生成英文 Word 文档

若仍需最终排版的 docx，可对润色后的译文运行：

```bash
python build_docx.py output/我的新论文-初稿/我的新论文-初稿_translation_humanized.md
```

- **输入：** `output/<输入文件名>/<输入文件名>_translation_humanized.md`
- **输出：** `output/<输入文件名>/<输入文件名>_translation_humanized.docx`（自动生成到同一目录）
- 字体 Times New Roman、正文 12 pt、参考文献 10 pt。
- **上下标自动转换**：Unicode 上下标字符自动转为 Word 原生上下标格式。
- **表格自动转换**：Markdown 表格自动转为 Word 原生表格，表头自动加粗。

打开生成的 docx 后核对：标题 / Abstract / Keywords 是否齐全、章节编号是否正确、字体字号是否合规、图表编号是否保留、上下标是否正确显示为 Word 原生格式、表格是否正确转换并格式良好。

---

## 三、翻译原则约定

1. **参考文献保持原样**：References 部分不翻译、不润色，保留原中文版格式（含 DOI、作者、期刊等）。
2. **专业术语规范**：常见英文缩写（如 BSA-AuNCs、HPLC、LC-MS/MS、XPS、CIE 等）直接使用。
3. **学术风格**：Nature 系列期刊风格，Times New Roman，正文 12 pt，参考文献 10 pt。
4. **图表编号保留**：Figure 1–N、Table 1–N、Scheme 1、Figure S1– 等在正文中保留以便对照。
5. **润色不改义**：humanizer 只去 AI 痕迹、调整表达，不得改变实验数据、结论与术语。
6. **表格格式统一**：所有表格使用 Markdown 标准语法，翻译时保持表格结构，只翻译单元格内容。

---

## 四、参考文档索引

以下文件已从本文档拆出，AI 助手按需 Read 即可，无需预先加载。

| 文件                           | 内容                                      | 何时需要                                    |
| ------------------------------ | ----------------------------------------- | ------------------------------------------- |
| `docs/环境准备.md`           | Conda/venv 环境配置教程、依赖分层说明     | 仅首次使用或 `.project_env.json` 不存在时 |
| `docs/对照表格式.md`         | `humanize_comparison.md` 的推荐格式模板 | 步骤 5（生成对照表）时                      |
| `docs/表格处理说明.md`       | 表格提取 / 翻译 / 生成的完整说明与示例    | 原文含表格时（步骤 3）                      |
| `docs/常见操作与脚本说明.md` | 常见操作速查表、各脚本 / skill 功能说明   | 按需查阅                                    |
