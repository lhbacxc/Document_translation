# 中文论文英译工作流 - 开发历程

## 项目背景

**创建时间：** 2026年6月

**项目目标：** 构建一个可复用的中→英学术论文翻译工作流,实现从中文 docx 到英文 docx 的完整转换流程,并确保译文质量达到学术发表标准。

**核心需求：**
- 保留原文格式(上下标、表格结构)
- 去除 AI 翻译痕迹,使译文更像人类手笔
- 提供 AI 痕迹检测能力
- 支持可复用的模板化流程

---

## 开发阶段记录

### 第一阶段: 基础工作流搭建 (2026/06/22)

#### 遇到的问题
1. **Word 文档解构问题**
   - 原始 docx 格式复杂,直接读取会丢失格式信息
   - 化学式中的上下标(如 H₂O、Cu²⁺)在提取时容易丢失
   - 需要一种中间格式来承载内容

#### 解决思路
- **采用 Markdown 作为中间格式**
  - 人类可读可编辑
  - 便于版本控制和对比
  - 易于程序化处理

- **开发 `extract_text.py` 脚本**
  - 解压 docx 文件,直接解析 `word/document.xml`
  - 识别 Word 的 `<w:vertAlign>` 标记,将上下标转为 Unicode 字符
  - 上标映射: `0-9` → `⁰¹²³⁴⁵⁶⁷⁸⁹`, `+-` → `⁺⁻`
  - 下标映射: `0-9` → `₀₁₂₃₄₅₆₇₈₉`, `+-` → `₊₋`
  - 输出纯文本到 `temp/extracted_text.txt`

- **开发 `build_docx.py` 脚本**
  - 解析 Markdown 标题、段落
  - 将 Unicode 上下标字符反向转换为 Word 原生格式
  - 使用 `python-docx` 库生成格式化的 Word 文档
  - 设置字体: Times New Roman, 正文 12pt, 参考文献 10pt

#### 技术要点
```python
# 上下标转换核心逻辑
_TO_SUPERSCRIPT = str.maketrans("0123456789+-", "⁰¹²³⁴⁵⁶⁷⁸⁹⁺⁻")
_TO_SUBSCRIPT = str.maketrans("0123456789+-", "₀₁₂₃₄₅₆₇₈₉₊₋")

# 在 build_docx 中识别并设置 Word 原生上下标
run.font.superscript = True  # 或 run.font.subscript = True
```

#### 阶段成果
- ✅ 基础的 docx → Markdown → docx 转换流程
- ✅ 上下标信息保留和还原
- ✅ 可复用的脚本工具
- ⚠️ 尚未支持表格处理

---

### 第二阶段: 引入 AI 痕迹处理 (2026/06/23)

#### 遇到的问题
1. **AI 翻译痕迹明显**
   - 直接 AI 翻译产生的英文充满套话短语(`play a crucial role`, `delve into`)
   - 过度使用 em dash (`—`)
   - 规则三连、同义词循环等典型 AI 写作特征
   - 学术期刊审稿时容易被识别为 AI 生成内容

2. **工作流效率问题**
   - 原方案将英文译文硬编码在 Python 脚本中
   - 每次翻译新论文都要修改脚本
   - 不便于复用和维护

#### 解决思路
- **集成 `humanizer` skill**
  - 利用现有的 AI 写作痕迹去除工具
  - 识别 30+ 类 AI 痕迹特征(基于 Wikipedia 的"Signs of AI writing"指南)
  - 逐段润色,保持段落数一致
  - 专业术语、数值、图表编号原样保留

- **引入 `paper-detection` skill**
  - 对润色后的译文进行 AI 痕迹检测
  - 逐句评估 AI 生成概率(0-100%)
  - 输出全文综合概率报告
  - 提供文档级信号(句长突发度、过渡词占比等)

- **重构工作流架构**
  - 将翻译内容与脚本分离
  - 统一用 Markdown 文件承载各阶段产物:
    - `translation.md` - 原始译文
    - `translation_humanized.md` - 润色后译文
    - `humanize_comparison.md` - 润色前后对照
    - `ai_detection_report.md` - AI 检测报告

#### 新增文档结构
```
translation.md                  ← 原始英文译文
      │
      │  /humanizer skill
      ├──────────────► translation_humanized.md    ← 润色译文
      └──────────────► humanize_comparison.md      ← 对照表
                             │
                             │  /paper-detection skill
                             ▼
                     ai_detection_report.md         ← 检测报告
                             │
                             │  build_docx.py
                             ▼
                     output/English_Translation_Complete.docx
```

#### 遇到的新问题
3. **长文本处理卡死**
   - 整篇论文一次性调用 humanizer 时推理时间过长
   - humanizer 规则注入本身就很长(30+ 类规则)
   - 叠加长输入和复杂对照输出,容易超时或卡死

#### 解决方案
- **分章节分批润色策略**
  - 按论文的一级/二级标题切分
  - 每批控制在 3-6 段或 800-1500 词
  - 长章节(如 Results、含大表格的小节)单独处理
  - References 章节单独作为最后一批,原样追加不润色

- **批次操作规范**
  - 第一批用 Write 创建文件
  - 后续批次用 Edit 追加到文件末尾
  - 使用 TodoWrite 跟踪批次进度
  - 对照表随每批润色同步追加,避免重复阅读长文本

- **完成后校验**
  - 用 grep 检索正文中的 `—`(em dash),确认已替换
  - 数值范围中的 en dash `–`(如 `0.01–15 μM`)保留
  - 校验化学式下标,确认使用 Unicode 下标而非 ASCII 数字

#### 阶段成果
- ✅ 完整的"翻译 → 润色 → 对照 → 检测"流程
- ✅ AI 痕迹去除能力
- ✅ 可复用的模板化工作流
- ✅ 长文本分批处理机制
- ✅ 详细的项目概览文档(`项目概览.md`)

---

### 第三阶段: 表格支持增强 (2026/06/24)

#### 遇到的问题
1. **表格完全丢失**
   - 原始 `extract_text.py` 无法识别 Word 表格
   - 提取的文本中表格内容散乱或缺失
   - 翻译后的 Markdown 无法还原表格结构
   - 生成的 docx 中表格消失

2. **表格格式复杂性**
   - Word 表格在 XML 中以 `<w:tbl>` 元素表示
   - 需要解析行(`<w:tr>`)和单元格(`<w:tc>`)结构
   - 表格内可能包含上下标、特殊字符
   - 需要在 Markdown 和 Word 之间双向转换

#### 解决思路

**步骤 1: 增强提取能力 (`extract_text.py`)**
- 识别 Word XML 中的 `<w:tbl>` 表格元素
- 遍历表格的行和单元格,提取文本
- 转换为 Markdown 表格格式:
  ```markdown
  | 列1 | 列2 | 列3 |
  | --- | --- | --- |
  | 数据1 | 数据2 | 数据3 |
  ```
- 保持表格在文档中的原始位置(段落间穿插)
- 表格单元格内的上下标同样转为 Unicode 字符

**核心实现**
```python
def _extract_table(tbl_elem) -> str:
    """从 Word 表格元素提取并转换为 Markdown 表格"""
    rows = []
    for tr in tbl_elem.findall(f"{W_NS}tr"):
        cells = []
        for tc in tr.findall(f"{W_NS}tc"):
            cell_text = _extract_cell_text(tc)  # 支持上下标
            cells.append(cell_text)
        if cells:
            rows.append(cells)
    
    # 构建 Markdown 表格
    md_lines = []
    for i, row in enumerate(rows):
        md_lines.append("| " + " | ".join(row) + " |")
        if i == 0:  # 第一行后添加分隔符
            md_lines.append("| " + " | ".join(["---"] * len(row)) + " |")
    
    return "\n".join(md_lines)
```

**步骤 2: 增强生成能力 (`build_docx.py`)**
- 在 Markdown 解析时识别表格行(以 `|` 开始和结束)
- 跳过分隔符行(`| --- | --- |`)
- 将表格数据转换为 Word 原生表格
- 应用表格样式(`Light Grid Accent 1`)
- 表头自动加粗
- 表格单元格内支持上下标转换

**核心实现**
```python
def add_table(doc: Document, rows: list, size: Pt = BODY_SIZE):
    """添加表格到 Word 文档"""
    table = doc.add_table(rows=len(rows), cols=len(rows[0]))
    table.style = 'Light Grid Accent 1'
    
    for i, row_data in enumerate(rows):
        row_cells = table.rows[i].cells
        for j, cell_text in enumerate(row_data):
            cell = row_cells[j]
            cell.text = ''
            p = cell.paragraphs[0]
            _add_runs_with_scripts(p, cell_text.strip(), size)  # 支持上下标
            if i == 0:  # 表头加粗
                for run in p.runs:
                    run.bold = True
```

**步骤 3: 测试验证**
- 创建 `input/表格测试.docx` 用于测试
- 测试复杂表格:多列、含上下标、科学计数法
- 生成 `table_translation_test.md` 验证端到端流程
- 确认表格在各阶段正确保留和转换

#### 技术难点
1. **表格与段落混排**
   - 需要按 XML 元素顺序遍历,保持表格在文档中的正确位置
   - 用统一的 `blocks` 列表存储段落和表格

2. **表格分隔符识别**
   - Markdown 表格的分隔符行(`| --- | --- |`)不应被视为数据行
   - 需要正则表达式精确识别: `all(re.match(r"^-+$", cell.strip())...)`

3. **表格状态跟踪**
   - 解析时需要 `in_table` 标志跟踪当前是否在表格中
   - 遇到非表格行时,需先 flush 当前表格

4. **单元格合并问题**
   - Word 的合并单元格在提取时会被拆分
   - 当前实现不支持单元格合并(这是已知限制)

#### 阶段成果
- ✅ 完整的表格提取、转换、生成能力
- ✅ 表格与正文混排支持
- ✅ 表格内上下标支持
- ✅ 表格自动样式化(表头加粗、应用 Word 样式)
- ✅ 在 `项目概览.md` 中新增"表格处理说明"章节
- ⚠️ 不支持单元格合并(已知限制,记录在文档中)

---

### 第四阶段: 基于输入文件名的自动命名 (2026/06/24)

#### 遇到的问题
1. **文件命名固定导致多论文处理冲突**
   - 所有生成文件使用固定名称（`translation.md`、`translation_humanized.md` 等）
   - 处理多篇论文时会相互覆盖
   - 无法从文件名直观识别对应的原始论文
   - 需要手动管理和重命名文件

2. **工作流可复用性受限**
   - 同一目录下只能处理一篇论文
   - 无法并行处理多个翻译项目
   - 文件关联不清晰，容易混淆

#### 解决思路

**核心策略：从输入文件名提取基础名称，作为所有生成文件的前缀**

**步骤 1: 修改 `extract_text.py`**
- 使用 `os.path.basename()` 和 `os.path.splitext()` 提取输入文件名（不含扩展名）
- 将提取的基础名称作为输出文件前缀
- 默认输出路径从 `temp/extracted_text.txt` 改为 `temp/<输入文件名>_extracted_text.txt`

```python
# 核心实现
input_basename = os.path.splitext(os.path.basename(docx_path))[0]
default_out = os.path.join("temp", f"{input_basename}_extracted_text.txt")
```

**步骤 2: 修改 `build_docx.py`**
- 从输入 markdown 文件名推导输出 docx 文件名
- 默认输出路径从固定的 `output/English_Translation_Complete.docx` 改为 `output/<输入文件名>.docx`
- 自动保持文件名一致性（例如 `论文A_translation_humanized.md` → `论文A_translation_humanized.docx`）

```python
# 核心实现
input_basename = os.path.splitext(os.path.basename(md_path))[0]
default_out = os.path.join("output", f"{input_basename}.docx")
```

**步骤 3: 更新文档和工作流说明**
- 更新 `项目概览.md` 中所有涉及文件名的章节
- 将固定文件名（`translation.md`）替换为动态格式（`<输入文件名>_translation.md`）
- 更新目录结构说明，新增文件命名规则
- 更新步骤 2-7 的示例命令和文件路径
- 更新文件流转图，使用实际文件名示例
- 更新常见操作表格

#### 技术实现细节

**文件名提取逻辑**
```python
# 从完整路径提取基础名称
# input/我的新论文-初稿.docx → 我的新论文-初稿
input_basename = os.path.splitext(os.path.basename(docx_path))[0]
```

**命名规则**
- 提取的文本：`<输入文件名>_extracted_text.txt`
- 原始译文：`<输入文件名>_translation.md`
- 润色译文：`<输入文件名>_translation_humanized.md`
- 对照表：`<输入文件名>_humanize_comparison.md`
- 检测报告：`<输入文件名>_ai_detection_report.md`
- 最终 docx：`<输入文件名>_translation_humanized.docx`

**向后兼容性**
- 脚本仍然支持自定义输出路径（命令行第二参数）
- 当用户明确指定输出路径时，使用指定路径
- 只有在未指定输出路径时才使用基于输入文件名的默认路径

#### 使用示例

**处理单篇论文**
```bash
# 输入：input/我的新论文-初稿.docx
python extract_text.py input/我的新论文-初稿.docx
# 输出：temp/我的新论文-初稿_extracted_text.txt

# 翻译后创建：我的新论文-初稿_translation.md
# 润色后生成：我的新论文-初稿_translation_humanized.md

python build_docx.py 我的新论文-初稿_translation_humanized.md
# 输出：output/我的新论文-初稿_translation_humanized.docx
```

**同时处理多篇论文**
```bash
# 论文 A
python extract_text.py input/论文A.docx
# → temp/论文A_extracted_text.txt, 论文A_translation.md, ...

# 论文 B
python extract_text.py input/论文B.docx
# → temp/论文B_extracted_text.txt, 论文B_translation.md, ...

# 文件不会相互覆盖，可以并行处理
```

#### 阶段成果
- ✅ 自动基于输入文件名生成所有中间和最终文件
- ✅ 支持同一目录下处理多篇论文
- ✅ 文件关联关系清晰，便于管理
- ✅ 保持向后兼容，支持自定义输出路径
- ✅ 完整更新项目文档和使用说明

---

## 当前项目状态 (2026/06/24)

### 已完成功能
- ✅ docx 解构提取(含上下标、表格)
- ✅ Markdown 中间格式管理
- ✅ AI 翻译痕迹去除(humanizer)
- ✅ 润色前后对照表生成
- ✅ AI 痕迹检测报告(paper-detection)
- ✅ 表格完整支持(提取、翻译、生成)
- ✅ 上下标双向转换
- ✅ 分批润色机制(避免长文本卡死)
- ✅ 基于输入文件名的自动命名(支持多论文并行处理)
- ✅ 完整的项目文档(`项目概览.md`、`开发历程.md`)

### 待优化项 (记录在 `TODO.md` 中)

#### 1. 图片支持
**问题描述:**
- 当前生成的英文 docx 只包含文字
- 原始 docx 中的图片未被提取和转移
- 需要手动在生成的文档中配图

**可能方案:**
- 从原始 docx 的 `word/media/` 目录提取图片
- 在 Markdown 中用占位符标记图片位置
- build_docx 时将图片插入相应位置
- 需要处理图片编号、图注等

**技术挑战:**
- 图片在 Word XML 中以 `<w:drawing>` 表示,解析复杂
- 需要维护图片与正文的对应关系
- 图片尺寸、对齐方式需要保留

#### 2. 文献引用域代码
**问题描述:**
- 生成的 docx 中文献引用是纯文本
- 不具备 Zotero 的域代码(field code)
- 无法通过 Zotero 实时编辑和更新引用

**当前方案:**
- References 章节保持原中文格式不翻译
- 在生成的英文 docx 中手动使用 Zotero 添加引用

**理想方案:**
- 保留或重建 Word 域代码
- 需要理解 Zotero 域代码结构
- 可能需要 Zotero API 支持

#### 3. 批量处理能力
**问题描述:**
- input 目录中有多个待翻译文件时
- 需要手动指定每个文件路径
- 流程串行,效率低

**期望功能:**
- 自动识别 input 目录中的所有 docx 文件
- 为每个文件创建独立的工作目录
- 按顺序完成每个文件的完整流程
- 生成的 docx 默认先不输出,仅生成 Markdown 产物

#### 4. Skill 依赖文档化
**问题描述:**
- `humanizer` 和 `paper-detection` skill 有独立的依赖环境
- `paper-detection` 使用独立的 conda 环境 `PDF_AIGC`
- 依赖信息需要在项目文档中明确说明

**需要补充:**
- 各 skill 的环境依赖列表
- 环境配置步骤
- 常见问题排查

---

## 技术栈总结

### 核心技术
- **Python 3.11**
  - 使用 miniconda 管理虚拟环境
  - 独立环境安装项目依赖

### 主要依赖
```txt
python-docx    # Word 文档生成和格式化
```

### 外部 Skills
- **humanizer** - AI 写作痕迹去除
  - 基于 Wikipedia "Signs of AI writing" 指南
  - 识别 30+ 类 AI 痕迹特征
  
- **paper-detection** - AI 痕迹检测
  - 独立 conda 环境: `PDF_AIGC`
  - 包含 spacy + en_core_web_sm
  - 提供逐句概率评估

### 数据流
```
Word (docx) → XML 解析 → Unicode 文本 → Markdown → 
AI 翻译 → Markdown 译文 → Humanizer 润色 → 
Markdown 润色稿 → Paper Detection → 检测报告 → 
Word 生成 (可选)
```

---

## 开发经验总结

### 成功经验

1. **Markdown 作为中间格式的选择**
   - 人类可读,便于手工编辑和校对
   - 纯文本格式,易于版本控制
   - 简洁的语法,易于程序化解析
   - 适合承载学术文本的结构化内容

2. **Unicode 字符转换策略**
   - 避免了复杂的 Word XML 操作
   - 在纯文本层面保留格式信息
   - 双向转换逻辑清晰、可靠
   - 支持化学、物理等领域的特殊符号

3. **分批处理机制**
   - 解决了长文本推理超时问题
   - 降低了单次调用的复杂度
   - 使进度可追踪、可恢复
   - 提高了整体工作流的稳定性

4. **内容与脚本分离**
   - 脚本成为真正可复用的工具
   - 翻译内容在 Markdown 中独立管理
   - 便于多人协作和版本对比
   - 降低了使用门槛

5. **基于输入文件名的自动命名**
   - 解决了多论文处理时的文件冲突问题
   - 文件关联关系一目了然
   - 支持在同一目录下并行处理多个项目
   - 保持向后兼容，不影响现有使用习惯

### 踩过的坑

1. **表格处理的复杂性**
   - 最初未考虑表格,导致重要信息丢失
   - Word 表格的 XML 结构比段落复杂得多
   - 需要同时处理表格与段落的混排
   - 分隔符行识别容易与普通分隔线混淆

2. **长文本一次性处理**
   - humanizer 在长文本上容易超时
   - 规则注入本身占用大量 token
   - 需要分批策略而非技术优化

3. **上下标字符集覆盖**
   - 最初只支持数字 0-9
   - 后来发现需要 `+-=()`、希腊字母等
   - 逐步扩展字符映射表
   - 需要根据实际论文内容调整

4. **References 章节的特殊性**
   - 参考文献不应翻译和润色
   - 需要在各环节单独判断和处理
   - 字号使用 10pt 而非 12pt
   - 格式保持原样(含中文作者、期刊名)

### 设计原则

1. **职责分离**
   - 提取、翻译、润色、检测、生成各自独立
   - 每个阶段产生明确的中间产物
   - 便于调试和局部修改

2. **数据驱动**
   - 脚本不写死任何论文内容
   - 所有内容从 Markdown 文件读取
   - 配置参数在脚本顶部集中定义

3. **可复现性**
   - 完整的文档记录每个步骤
   - 中间产物全部保留便于审查
   - 对照表记录所有改动及理由

4. **渐进增强**
   - 先实现核心功能(文本转换)
   - 再逐步增加高级特性(表格、润色、检测)
   - 每个阶段都是可工作的完整流程

---

## 文件清单

### 核心脚本
- `extract_text.py` - docx 解构提取工具(148 行)
- `build_docx.py` - Markdown 转 docx 工具(270 行)

### 配置文件
- `requirements.txt` - Python 依赖清单
- `项目概览.md` - 完整的项目文档和使用说明(336 行)
- `TODO.md` - 待优化项清单

### 数据目录
- `input/` - 待翻译的中文 docx
- `temp/` - 中间产物(提取的文本等)
- `output/` - 最终生成的英文 docx

### 工作流产物
- `<输入文件名>_translation.md` - 原始英文译文
- `<输入文件名>_translation_humanized.md` - 润色后英文译文
- `<输入文件名>_humanize_comparison.md` - 润色前后对照表
- `<输入文件名>_ai_detection_report.md` - AI 痕迹检测报告

### 测试文件
- `table_translation_test.md` - 表格功能测试用例

---

## 后续规划

### 短期目标
1. 补充图片提取和插入功能
2. 完善批量处理能力
3. 优化 Zotero 域代码支持
4. 完善依赖环境文档

### 长期愿景
1. 开发 GUI 界面,降低使用门槛
2. 支持更多文档格式(PDF、LaTeX)
3. 集成翻译 API,实现端到端自动化
4. 提供云端服务,支持团队协作

---

*文档生成时间: 2026/06/24*
*基于项目文件和代码分析整理*
*最后更新: 2026/06/24 - 新增第四阶段（基于输入文件名的自动命名）*
